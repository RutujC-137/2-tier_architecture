version: 0.2

phases:
  install:
    commands:
      - echo "Installing frontend dependencies..."
      - cd frontend && npm install
  build:
    commands:
      - echo "Building frontend..."
      - npm run build
  post_build:
    commands:
      - echo "Deploying Frontend..."
      - cd .. && bash scripts/frontend-deploy.sh
